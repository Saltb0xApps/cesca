DATABASE_URL="file:./dev.db"

# Rot tuning — see src/lib/rot.ts
ROT_DECAY_RATE=0.05
ROT_REST_RATE=0.02
ROT_GRAFT_FACTOR=0.5

# DEMO_MODE cranks decay so a plant visibly rots in ~60s on stage.
DEMO_MODE=false
DEMO_DECAY_MULT=25
